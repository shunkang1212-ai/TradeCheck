# v0.1.1 验证记录

验证日期：2026-09-06（Asia/Jakarta）

## 已验证

- `python -B -m unittest discover -s tests`：106 个测试通过。
- 浏览器流程：上传虚构 Excel、提取预览、确认批次、发现短装 20 件、人工修正、
  隐藏旧结果、重新核对、生成并预览报告。
- 输入保护：核对前后样例 SHA-256 不变。
- 页面控制台：走查期间未发现错误或警告。
- GitHub Actions（2026-09-07）：Python 3.9、3.12、3.13 三个任务全部通过。

## 未验证

- 真实客户单据上的准确率。
- 接近 20 MB 或 5000 行上限时的性能。
- 非常规合并单元格、多工作表、多发票和退货冲抵。
- Windows 与 Linux 的人工界面走查。

测试通过只能证明已覆盖场景中的实现行为符合预期，不代表所有外贸单据都能正确识别。
