# 安全说明

## 支持范围

当前仅维护最新发布版本。项目仍处于 alpha 阶段，不应作为付款、报关、合同履行或
其他关键业务决定的唯一依据。

## 报告安全问题

请通过 GitHub 仓库的私密安全报告功能联系维护者。公开 Issue 中不要包含真实单据、
客户资料、银行信息、访问令牌或可识别个人的信息。

报告应包含受影响版本、复现步骤、影响说明和不含敏感信息的最小样例。维护者确认前，
请不要公开可直接利用的细节。

## 数据边界

TradeCheck 默认只监听 `127.0.0.1`，不向外部服务发送上传内容，也不修改源文件。
生成的 HTML 报告可能包含原单据字段，使用者仍需妥善存放和删除本地报告。

