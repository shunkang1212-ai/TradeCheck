# 参与贡献

感谢你帮助改进 TradeCheck。当前项目处于 alpha 阶段，最有价值的贡献是新增可复现的
单据布局、边界用例和小范围修复。

## 提交问题

- 说明使用的模板、预期结果和实际结果。
- 只附虚构或彻底脱敏的文件；删除客户、价格、银行、签章、联系人和订单信息。
- 不要在公开 Issue 中提交真实商业单据、密码、令牌或其他机密资料。
- 能用 `tools/make_samples.py` 生成最小复现样例时，优先提交生成脚本的修改。

## 本地开发

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -e .
python -m unittest discover -s tests
```

## Pull Request 要求

1. 每个 PR 聚焦一个问题，说明修改前后的行为。
2. 规则变化需要加入独立预期值的测试，不能用被测函数生成预期结果。
3. 保持本地运行、不上传文件、不修改原始工作簿。
4. 涉及金额时继续使用 `Decimal`，并明确币种、小数位和容差。
5. 运行全部测试，并在 PR 中写明结果。

维护者会按正确性、数据安全、兼容性和范围大小审查贡献。

